# Tamaki Studio v4.0 実装計画書

本ドキュメントは、Tamaki Studioの次期メジャーアップデート **v4.0** における「表記・誤字脱字チェック機能」「たまき流推敲スタジオ機能」、および「たまきぱずず語録・思考トリガーの1行選択窓化」の実装計画書です。

---

## 📌 背景と目的

Tamaki Studioは、たまきぱずず氏特有の文体（鋭いメタ認知、高い解像度、思考の迷走、軽妙なオチ）を再現する専用執筆支援ツールとして発展してきました。
v3.0系では自動保存・Mac↔iPhoneクラウド同期やChatGPT連携、便所の落書き機能が確立されましたが、**「完成した文章の表記ゆれ・誤字脱字を精査し、文体のリズムとキレを極限まで研ぎ澄ます」**という最終工程は外部ツールや手作業に頼っていました。

v4.0では、以下の3つを核として実装します：
1. **画面の劇的スリム化**: 大量のボタンで縦領域を圧迫していた「語録・思考トリガー」を**1行のドロップダウン選択窓**に刷新。
2. **高速ローカル校正エンジン**: API不要・完全ブラウザ完結で、誤字脱字・表記揺れ・ら抜き言葉・1文の長すぎをリアルタイム検知。
3. **たまき流・推敲スタジオ（Gemini AI & Diff）**: 既存のGemini APIを活用し、筆者の持ち味を殺さずに贅肉を削ぎ落とす推敲案を生成。差分（Diff）比較とワンクリック反映を提供。

---

## 🛠️ 主な改修・追加機能の構成

```
【UI改善】
語録・思考トリガー（旧: 縦長チップ群）
   │
   ▼（95%省スペース化）
1行セレクトボックス ［🧩 語録を選択して挿入... ▼］

【校正・推敲エンジン】
執筆原稿（エディタ入力）
   ├── 【Level 1】即時ルールベース校正（完全ローカル・0秒検知）
   │     └── 表記揺れ / 誤字脱字 / ら抜き / 助詞重複 / 長文警告 / コンプラ身バレ検知
   │
   └── 【Level 2】Gemini AI推敲スタジオ（文脈解析・たまき節強化）
         └── Diff（差分）ビューア / たまき節ブラッシュアップ / ワンクリック反映
```

---

## 📂 変更対象ファイル一覧

| ファイル | 役割・変更内容 |
| :--- | :--- |
| `index.html` | ① 語録パレットを1行`<select>`化<br>② エディタ周辺に「校正・推敲ドック」「Diffモーダル/パネル」を追加<br>③ バージョン表記を `v4.0.0` に更新 |
| `styles.css` | ① 1行選択窓のスタイリング（ダークテーマ・省スペース）<br>② 校正インスペクター（指摘バッジ・カード）のスタイル<br>③ Diff差分表示（追加: 緑、削除: 赤ハイライト）のスタイル<br>④ iPhone/PWA表示のレスポンシブ最適化 |
| `app.js` | ① `renderPalette()` を1行ドロップダウン生成＆選択挿入ロジックに変更<br>② クライアントサイド校正エンジン（辞書＋正規表現ルール＋debounce）<br>③ Gemini API推敲リクエスト（客観校正／たまき節リライトの2モード）<br>④ テキスト差分（Diff）生成およびワンクリック反映処理 |
| `README.md` | v4.0.0 の新機能説明、仕様、操作ガイドの追記 |
| `sw.js` | キャッシュバージョンを `tamaki-studio-v4.0.0` に更新（PWA自動更新対応） |

---

## 📋 詳細仕様

### 1. 語録・思考トリガーの1行選択窓化（案A）
* **UI構造**: 
  ```html
  <div class="phrase-select-wrapper">
    <select id="phrase-select" class="phrase-select-dropdown">
      <option value="">🧩 語録・思考トリガーを選択して現在の欄に挿入...</option>
      <!-- optgroup ごとに分類（共通、エッセイ、サブカル、SF等） -->
    </select>
  </div>
  ```
* **挙動**:
  * ユーザーがフレーズを選ぶと、直前にフォーカスしていた入力欄（未選択なら現在モードの主入力欄）にフレーズを自動挿入。
  * 挿入直後に選択状態は初期値（プレースホルダー）にリセットされ、次の選択に備える。
  * これにより、画面の縦専有面積を約 **95%削減**（数十pxの1行に圧縮）。

### 2. 即時ルールベース校正エンジン（完全ローカル）
* **動作タイミング**: テキストエリアの入力時（400ms debounce）。API不要で完全オフライン動作。
* **検知カテゴリ**:
  1. **表記揺れ検知**: 「ウェブ／Web」「ユーザー／ユーザ」「行う／行なう」「取り組む／取組む」「コンピューター／コンピュータ」等の混在。
  2. **日本語の誤用・慣用句チェック**:
     * 「的を得る」→「的を射る」
     * 「敷居が高い」→「ハードルが高い／不義理で合わせる顔がない」
     * 「足をすくわれる」→「足元をすくわれる」
     * 「汚名挽回」→「名誉挽回／汚名返上」
     * 「情けは人のためならず」の文脈等の注意喚起
  3. **悪文・リズムの引っ掛かり**:
     * **ら抜き言葉**: 「見れる（→見られる）」「食べれる（→食べられる）」「来れる（→来られる）」
     * **助詞の連続**: 「〜の〜の〜の」（1文中に「の」が3回以上連続）
     * **1文の長すぎ警告**: 1文が110文字以上続いている場合に注意喚起（息切れ防止）
     * **二重否定**: 「〜なくもない」「〜ないわけではない」
  4. **コンプラ・身バレ語句**: 既存の車種名（マークII、セルシオ等）やホテル名検知を校正インスペクター内に統合。
* **UI**:
  * エディタ上部に `[ 🔍 校正チェック: ⚠️ 3件の指摘 ]` バッジ。
  * クリックで展開し、指摘内容（修正理由）とワンクリック修正ボタンを提供。

### 3. たまき流・推敲スタジオ（Gemini AI & Diff）
* **2つの推敲モード**:
  * **【モードA：精密校正】**: 誤字脱字、文脈依存の同音異義語（「意外/以外」「関心/感心」等）、主語・述語のねじれを検出し、解説とともに修正。
  * **【モードB：たまき節・推敲】**: 筆者の独特な「自虐・メタ認知」「思考の脱線」「生々しいリアリズム」「軽妙なオチ」を活かしつつ、文章の贅肉・重複・リズムの悪さだけを削ぎ落としてテンポを向上させる。
* **Diff（差分）ビューア**:
  * 推敲前後のテキストを比較し、赤（削除）と緑（追加）でハイライト。
  * 「💡 編集者からの推敲メモ（リライトの意図）」を表示。
  * 「✨ この推敲案をエディタに反映する」ボタンで即座にエディタへ適用。
* **ChatGPTバトンタッチ強化**: APIキー未設定の場合でも、同じ推敲方針を盛り込んだプロンプトをクリップボードにコピー可能。

---

## 🧪 検証・テスト計画

1. **語録選択窓のテスト**:
   * PCおよびiPhone実機（Safari）でセレクトボックスをタップし、選択したフレーズが各モード（エッセイ・サブカル・小説・便所の落書き）の適切な入力欄に挿入されること。
   * 挿入後に選択肢がプレースホルダー（初期状態）に戻ること。
2. **ルールベース校正のテスト**:
   * 「的を得た意見だったが見れるとは思わなかった。彼の友人の父の車で行った。」等のテスト文を入力し、「的を射る」「見られる」「の の連続」が正しくハイライト・警告されること。
   * 誤字修正をクリックすると正しくエディタ上で置換されること。
3. **Gemini推敲＆Diffのテスト**:
   * Gemini APIキーを設定した状態で「たまき節で推敲」を実行し、Diff画面が表示され、ワンクリック反映でエディタが更新されること。
4. **PWA・iPhone実機互換性テスト**:
   * スマホ画面幅（375px〜428px）で崩れがないか、サービスワーカー（sw.js）のキャッシュ更新が正常に行われるか確認。
